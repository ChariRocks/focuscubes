{
  "name": "focus-cubes",
  "version": "1.0.0",
  "description": "Focus and wellness app",
  "main": "server.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && node server.js",
    "dev": "npx nodemon -e js,html,css --ignore styles.compiled.css --exec 'npm run build && node server.js'"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.12.1",
    "puppeteer": "^24.4.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "nodemon": "^2.0.22",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.2"
  }
}
